#include <stdio.h>
#include <conio.h>
void main()
{
	int rev = 0, num, dight;
	printf ("enter the number")
	scanf ("%d", &n)
	num = n
	while (n!=0)
	{
	digits = n % 10
	rev = rev * 10 + digits
	num = num % 10;
}
if (rev==num)
printf ("the number is palindrome")
else
printf ("the number is not palindrome")
getch()
}
